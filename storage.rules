rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /uploads/{userId}/{allPaths=**} {
      // Allow access if the user is authenticated and matches the userId folder
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
